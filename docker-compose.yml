services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ai_report
      POSTGRES_PASSWORD: ai_report
      POSTGRES_DB: ai_report
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
